<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Sign In</title>

  <!-- ✅ Bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <style>
    body {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(135deg, #a8c0ff, #3f2b96);
      font-family: 'Poppins', sans-serif;
    }

    .auth-container {
      display: flex;
      flex-direction: column;
      width: 95%;
      max-width: 950px;
      height: auto;
      min-height: 580px;
      box-shadow: 0 10px 35px rgba(0, 0, 0, 0.2);
      border-radius: 16px;
      overflow: hidden;
      background-color: #fff;
      transition: all 0.3s ease-in-out;
    }

    .auth-container:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.25);
    }

    @media (min-width: 768px) {
      .auth-container {
        flex-direction: row;
        height: 580px;
      }
    }

    .auth-image {
      flex: 1;
      background: url("https://i.pinimg.com/736x/a8/80/69/a880697cdbebf5a4929a1bd7ddc1a35d.jpg") center/cover no-repeat;
      filter: brightness(0.9);
    }

    .auth-form {
      flex: 1;
      padding: 50px 45px;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .auth-form h1 {
      margin-bottom: 25px;
      font-weight: 700;
      color: #3f2b96;
      text-align: center;
      letter-spacing: 0.5px;
    }

    .form-control {
      border-radius: 10px;
      border: 1px solid #ddd;
      padding: 12px 15px;
      font-size: 15px;
      transition: all 0.2s ease-in-out;
      box-shadow: none;
    }

    .form-control:focus {
      border-color: #3f2b96;
      box-shadow: 0 0 6px rgba(63, 43, 150, 0.4);
    }

    .btn-primary {
      border-radius: 10px;
      font-weight: 600;
      letter-spacing: 0.5px;
      background: linear-gradient(90deg, #667eea, #764ba2);
      border: none;
      transition: all 0.3s ease-in-out;
    }

    .btn-primary:hover {
      background: linear-gradient(90deg, #5a67d8, #6b46c1);
      transform: translateY(-1px);
    }

    .auth-form a {
      text-decoration: none;
      color: #3f2b96;
      font-weight: 500;
    }

    .auth-form a:hover {
      text-decoration: underline;
    }

    .alert {
      border-radius: 10px;
      font-size: 14px;
    }

    p {
      font-size: 15px;
    }
  </style>
</head>
<body>

  <div class="auth-container">

    <div class="auth-image d-none d-md-block"></div>

    <div class="auth-form">
      <h1>Welcome Back</h1>

      <% if (message) { %>
        <div class="alert alert-danger py-2 text-center"><%= message %></div>
      <% } %>

      <form action="/users/signin" method="POST">
        <div class="mb-3">
          <input type="email" class="form-control" name="email" placeholder="Email Address" required>
        </div>
        <div class="mb-3">
          <input type="password" class="form-control" name="password" placeholder="Password" required>
        </div>
        <button type="submit" class="btn btn-primary w-100 py-2">Sign In</button>
      </form>

      <p class="mt-3 text-center">
        Don’t have an account? <a href="/users/signup">Sign Up</a>
      </p>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
